# Propuesta Vacaciones

Pequeña app React para proponer calendario de vacaciones.

Cómo ejecutar localmente:

```bash
npm install
npm run dev
# abrir http://localhost:5173/
```

Para subir a GitHub:

```bash
git remote add origin <URL-del-repositorio>
git branch -M main
git push -u origin main
```
